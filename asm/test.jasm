.prep
.body
    main:
        stc %i 15
        stc %f 15
        stc %b 15

    symbol:
        stc %i symbol 
        stc %f symbol 
        stc %b symbol 

        stc 15.0
        stc 15
        
        ldc %i
        ldc %f
        ldc %b

        rda &eax
        rda %i
        rda %f
        rda %b

        mov &eax
        mov &eax &ebx
        mov 15 &eax
        mov 15.0 &eax
.end
